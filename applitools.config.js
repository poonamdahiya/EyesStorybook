module.exports = {
  showLogs: true
//    tag: ({name, kind, parameters}) => {
//      return test(name)
//    }
}

//function getStoryMetaData() {
//const {name, kind, parameters} = story;
//const storyUrl = getStoryUrl(story, storybookUrl);
//return storyUrl;
//}

//  include: ({name, kind, parameters}) => {
//    return !/^\[SKIP\]/.test(name)
//  }
//  storybookUrl:	'http://localhost:9000'
//  properties: [{name: 'NEW PROP', value: 'value1'}]
//  properties: ({name, kind, parameters}) => {
//                const {name, kind, parameters} = story;
//                const storyUrl = getStoryUrl(story, storybookUrl);
//                  return storyUrl;
//                }

//const storybookBaseUrl = getStorybookBaseUrl(storybookUrl);
//  tag: ({name, kind, parameters}) => {
//  const {name, kind, parameters} = story;
//  const storyUrl = getStoryUrl(story, storybookUrl);
//    return storyUrl;
//  }